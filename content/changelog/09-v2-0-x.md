## v2.0.x 系列（2.0.76 – 2.0.0）

### 2.0.76

**修复**
- 修复在 Chrome 集成中使用 Claude 时出现的 macOS 代码签名（code-sign）警告问题

### 2.0.75

**修复**
- 若干小问题修复

### 2.0.74

**新功能**
- 新增 LSP（Language Server Protocol）工具，提供 go-to-definition、find references、hover 文档等代码智能功能
- 新增 `/terminal-setup` 对 Kitty、Alacritty、Zed 和 Warp 终端的支持
- 在 `/theme` 中新增 ctrl+t 快捷键用于切换语法高亮的开/关
- 在主题选择器中新增语法高亮信息
- 当 macOS 用户因终端配置导致 Alt 快捷键失效时，新增相应指引

**修复**
- 修复 skill 的 `allowed-tools` 未应用到该 skill 所调用工具的问题
- 修复用户已在使用 Opus 时仍错误显示 Opus 4.5 提示的问题
- 修复语法高亮未正确初始化时可能导致的崩溃
- 修复 `/plugins discover` 中的视觉错误：搜索框聚焦时列表选择指示器仍会显示
- 修复 macOS 键盘快捷键显示 'opt' 而非 'alt' 的问题
- [Windows] 修复渲染不正确的问题

**改进**
- 改进 `/context` 命令的可视化效果：按来源对 skills 与 agents 分组、展示 slash commands 并按 token 数量排序

**其他**
- [VSCode] 为年终促销消息添加礼物标签图标

### 2.0.73

**新功能**
- 新增可点击的 `[Image #N]` 链接，可在默认查看器中打开附带的图片
- 新增 alt-y yank-pop，可在 ctrl-y yank 后循环浏览 kill ring 历史
- 在插件 discover 界面新增搜索过滤（输入即可按名称、描述或 marketplace 过滤）
- 新增对 fork 会话时使用自定义 session ID 的支持，需将 `--session-id` 与 `--resume` 或 `--continue` 及 `--fork-session` 组合使用

**修复**
- 修复输入历史循环缓慢的问题，以及可能在消息提交后覆盖文本的竞态条件

**改进**
- 改进 `/theme` 命令，使其直接打开主题选择器
- 改进主题选择器 UI
- 通过统一的 SearchBox 组件改进了 resume 会话、permissions 和 plugins 各界面的搜索体验

**其他**
- [VSCode] 新增标签页图标徽标，显示待处理权限（蓝色）和未读完成项（橙色）

### 2.0.72

**新功能**
- 新增 Claude in Chrome（Beta）功能，配合 Chrome 扩展（https://claude.ai/chrome）使用，可直接从 Claude Code 控制你的浏览器
- 新增可扫描的二维码到移动应用提示中，便于快速下载应用
- 新增恢复对话时的加载指示器，提供更好的反馈

**修复**
- 修复 `/context` 命令在非交互模式下不遵循自定义系统提示词的问题
- 修复使用 Ctrl+Y 粘贴时连续 Ctrl+K 行的顺序问题

**改进**
- 减少终端闪烁
- 改进 @ mention 文件建议速度（在 git 仓库中约快 3 倍）
- 改进在包含 `.ignore` 或 `.rgignore` 文件的仓库中的文件建议性能
- 改进设置校验错误，使其更醒目
- 将 thinking 切换键从 Tab 改为 Alt+T，以避免误触发

### 2.0.71

**新功能**
- 新增 /config 开关，用于启用/禁用提示词建议
- 新增 `/settings` 作为 `/config` 命令的别名

**修复**
- 修复光标位于路径中间时 @ 文件引用建议被错误触发的问题
- 修复使用 `--dangerously-skip-permissions` 时 `.mcp.json` 中的 MCP 服务器未加载的问题
- 修复权限规则错误拒绝包含 shell glob 模式的有效 bash 命令的问题（例如 `ls *.txt`、`for f in *.png`）

**改进**
- Bedrock：在 token 计数和 inference profile 列举时现在会遵循环境变量 `ANTHROPIC_BEDROCK_BASE_URL`
- 为原生构建提供全新的语法高亮引擎

### 2.0.70

**新功能**
- 新增 Enter 键以立即接受并提交提示词建议（tab 仍用于接受以便编辑）
- 新增通配符语法 `mcp__server__*` 用于 MCP 工具权限，以允许或拒绝某个服务器的所有工具
- 为插件 marketplace 新增自动更新开关，可对每个 marketplace 单独控制自动更新
- 在状态行输入中新增 `current_usage` 字段，可实现精确的上下文窗口百分比计算

**修复**
- 修复用户正在输入时处理排队命令导致输入被清空的问题
- 修复按下 Tab 时提示词建议替换已输入内容的问题
- 修复终端调整大小时 diff 视图不更新的问题
- 修复 /config 中 thinking 模式切换未正确持久化的问题

**改进**
- 大型对话的内存占用改进了 3 倍
- 改进复制到剪贴板（Ctrl+S）的统计截图分辨率，使图像更清晰
- 移除用于快速记录 memory 的 # 快捷键（请让 Claude 编辑你的 CLAUDE.md）
- 改进文件创建权限对话框的 UI

### 2.0.69

**修复**
- 若干小问题修复

### 2.0.68

**修复**
- 修复中文、日文、韩文等语言的 IME（输入法编辑器）支持，正确将候选词窗口定位在光标处
- 修复被禁用的 MCP 工具仍对模型可见的 bug
- 修复 subagent 工作期间引导消息（steering messages）可能丢失的问题
- 修复 Option+方向键单词导航将整段 CJK（中文、日文、韩文）文本视为单个单词、而非按单词边界导航的问题

**改进**
- 改进 plan 模式退出体验：当退出时 plan 为空或缺失时，显示简化的 yes/no 对话框，而非抛出错误

**新功能**
- 新增对企业托管设置（enterprise managed settings）的支持。请联系你的 Anthropic 客户团队启用此功能。

### 2.0.67

**改进**
- Opus 4.5 现在默认启用 thinking 模式
- thinking 模式配置已移至 /config
- 在 `/doctor` 中显示 autoupdater 被禁用的原因

**新功能**
- 在 `/permissions` 命令中新增搜索功能，可用 `/` 快捷键按工具名过滤规则

**修复**
- 修复当另一实例已是最新版本时运行 `claude update` 仍出现错误的 "Another process is currently updating Claude" 提示
- 修复在非交互模式（`-p` 标志或管道输入）下运行时 `.mcp.json` 中的 MCP 服务器卡在 pending 状态的问题
- 修复在 `/permissions` 中删除权限规则后滚动位置被重置的问题
- 修复单词删除（opt+delete）和单词导航（opt+arrow）在西里尔文、希腊文、阿拉伯文、希伯来文、泰文和中文等非拉丁文本上无法正确工作的问题
- 修复 `claude install --force` 未绕过过期锁文件的问题
- 修复 CLAUDE.md 中连续 @~/ 文件引用因 markdown 删除线干扰而被错误解析的问题
- Windows：修复因日志目录路径中含冒号导致插件 MCP 服务器失败的问题

### 2.0.65

**新功能**
- 新增在编写提示词时通过 alt+p（linux、windows）、option+p（macos）切换模型的能力
- 在状态行输入中新增上下文窗口信息
- 新增 `fileSuggestion` 设置，用于自定义 `@` 文件搜索命令
- 新增 `CLAUDE_CODE_SHELL` 环境变量以覆盖自动 shell 检测（当登录 shell 与实际工作 shell 不同时很有用）

**修复**
- 修复用 Escape 中止查询时提示词未保存到历史的问题
- 修复 Read 工具图像处理从文件扩展名识别格式的问题，改为从字节识别

### 2.0.64

**改进**
- 使自动压缩（auto-compacting）即时完成
- 智能体（Agents）和 bash 命令可异步运行并发送消息唤醒主智能体
- 卸下了 AgentOutputTool 和 BashOutputTool，改用新的统一 TaskOutputTool

**新功能**
- /stats 现在向用户提供有趣的 CC 统计，例如最喜爱的模型、使用图表、使用连续记录
- 新增命名会话支持：使用 `/rename` 命名会话，在 REPL 中用 `/resume <name>` 或在终端用 `claude --resume <name>` 恢复它们
- 新增对 .claude/rules/ 的支持。详见 https://code.claude.com/docs/en/memory
- 新增图像缩放时的图像尺寸元数据，可为大图像实现精确的坐标映射

**修复**
- 修复使用原生安装器时自动加载 .env 的问题
- 修复使用 `--continue` 或 `--resume` 标志时 `--system-prompt` 被忽略的问题
- VSCode：修复扩展在 Windows ARM64 上无法工作的问题，改为通过模拟回退到 x64 二进制文件

**改进**
- 改进 `/resume` 界面：对 fork 出的会话进行分组，并新增预览（P）和重命名（R）的键盘快捷键
- VSCode：在代码块和 bash 工具输入上新增复制到剪贴板按钮
- Bedrock：提升 token 计数效率
- Bedrock：新增对 `aws login` AWS 管理控制台凭据的支持

### 2.0.62

**新功能**
- 为多选问题新增 "(Recommended)"（推荐）标识，并将推荐选项移至列表顶部
- 新增 `attribution` 设置以自定义提交和 PR 的署名（弃用 `includeCoAuthoredBy`）

**修复**
- 修复 ~/.claude 软链接到项目目录时出现重复 slash command 的问题
- 修复多个命令同名时 slash command 选择失效的问题
- 修复软链接的 skill 目录内的 skill 文件可能变成循环软链接的问题
- 修复因锁文件被错误判定为过期而导致正在运行的版本被移除的问题
- 修复拒绝文件更改时 IDE diff 标签页未关闭的问题

### 2.0.61

**改进**
- 由于响应速度问题，回退了 VSCode 对多个终端客户端的支持。

### 2.0.60

**新功能**
- 新增后台智能体支持。智能体在你工作时于后台运行
- 新增 --disable-slash-commands CLI 标志以禁用所有 slash command
- 在 "Co-Authored-By" 提交消息中新增模型名称
- 启用 "/mcp enable [server-name]" 或 "/mcp disable [server-name]" 以快速切换所有服务器

**改进**
- 更新 Fetch，对预先批准的网站跳过摘要

**其他**
- VSCode：新增对多个终端客户端同时连接到 IDE 服务器的支持

### 2.0.59

**新功能**
- 新增 --agent CLI 标志以覆盖当前会话的 agent 设置
- 新增 `agent` 设置以使用特定 agent 的系统提示词、工具限制和模型来配置主线程

**修复**
- VS Code：修复 .claude.json 配置文件从错误位置读取的问题

### 2.0.58

**新功能**
- Pro 用户现在可作为其订阅的一部分使用 Opus 4.5！

**修复**
- 修复计时器时长显示为 "11m 60s" 而非 "12m 0s" 的问题

**改进**
- Windows：托管设置现在优先使用 `C:\Program Files\ClaudeCode`（若存在）。对 `C:\ProgramData\ClaudeCode` 的支持将在未来版本中移除。

### 2.0.57

**新功能**
- 新增拒绝 plan 时的反馈输入，允许用户告诉 Claude 需要修改什么

**其他**
- VSCode：新增流式消息支持，用于实时响应显示

### 2.0.56

**新功能**
- 新增启用/禁用终端进度条（OSC 9;4）的设置
- VSCode 扩展：新增对 VS Code 次级侧边栏（VS Code 1.97+）的支持，可在保持文件资源管理器位于左侧的同时将 Claude Code 显示在右侧边栏。需要在配置中将侧边栏设为 Preferred Location。

### 2.0.55

**修复**
- 修复代理 DNS 解析默认被强制开启的问题。现在通过 `CLAUDE_CODE_PROXY_RESOLVES_HOSTS=true` 环境变量选择性开启
- 修复在 memory 位置选择器中长按方向键时键盘导航失去响应的问题

**改进**
- 改进 AskUserQuestion 工具，在最后一个问题上自动提交单选问题，从而省去简单问题流程中多余的复查界面
- 改进 `@` 文件建议的模糊匹配，结果更快更准确

### 2.0.54

**新功能**
- Hooks：使 PermissionRequest hooks 能够处理 'always allow' 建议并应用权限更新

**修复**
- 修复 iTerm 通知过多的问题

### 2.0.52

**修复**
- 修复以命令行参数启动 Claude 时出现重复消息显示的问题
- 修复 `/usage` 命令进度条的填充方式，使其随使用量增加而填充（而非显示剩余百分比）
- 修复在运行 Wayland 的 Linux 系统上无法粘贴图像的问题（当 xclip 不可用时现在回退到 wl-paste）

**改进**
- 允许在 bash 命令中使用某些 `$!` 用法

### 2.0.51

**新功能**
- 新增 Opus 4.5！https://www.anthropic.com/news/claude-opus-4-5
- 推出 Claude Code for Desktop：https://claude.com/download
- 为了给你留出体验新模型的空间，我们更新了 Claude Code 用户的使用限制。完整详情见 Claude Opus 4.5 博客
- Pro 用户现在可购买额外用量，以在 Claude Code 中使用 Opus 4.5

**改进**
- Plan 模式现在能构建更精确的 plan 并更彻底地执行
- 使用限制通知现在更易于理解
- 将 `/usage` 切换回 "% used"（已使用百分比）

**修复**
- 修复 thinking 错误的处理
- 修复性能回退问题

### 2.0.50

**修复**
- 修复阻止调用其输入 schema 中含嵌套引用的 MCP 工具的 bug
- 静默了升级期间一个嘈杂但无害的错误

**改进**
- 改进 ultrathink 文本显示
- 改进 5 小时会话限制警告消息的清晰度

### 2.0.49

**新功能**
- 新增 readline 风格的 ctrl-y 用于粘贴已删除的文本

**改进**
- 改进使用限制警告消息的清晰度

**修复**
- 修复 subagent 权限的处理

### 2.0.47

**改进**
- 改进 `claude --teleport` 的错误消息和校验
- 改进 `/usage` 的错误处理

**修复**
- 修复退出时历史条目未被记录的竞态条件
- 修复 Vertex AI 配置未从 `settings.json` 应用的问题

### 2.0.46

**修复**
- 修复当无法从元数据检测格式时图像文件被报告为错误媒体类型的问题

### 2.0.45

**新功能**
- 新增对 Microsoft Foundry 的支持！见 https://code.claude.com/docs/en/azure-ai-foundry
- 新增 `PermissionRequest` hook，可用自定义逻辑自动批准或拒绝工具权限请求
- 通过以 `&` 开头的消息将后台任务发送到 Claude Code on the web

### 2.0.43

**新功能**
- 为自定义 agents 新增 `permissionMode` 字段
- 为 `PreToolUseHookInput` 和 `PostToolUseHookInput` 类型新增 `tool_use_id` 字段
- 新增 skills frontmatter 字段，用于声明为 subagents 自动加载的 skills
- 新增 `SubagentStart` hook 事件

**修复**
- 修复 @-mention 文件时嵌套的 `CLAUDE.md` 文件未加载的问题
- 修复 UI 中某些消息重复渲染的问题
- 修复一些视觉闪烁
- 修复 NotebookEdit 工具在 cell ID 匹配 `cell-N` 模式时将 cell 插入错误位置的问题

### 2.0.42

**新功能**
- 为 `SubagentStop` hooks 新增 `agent_id` 和 `agent_transcript_path` 字段。

### 2.0.41

**新功能**
- 为基于提示词的 stop hooks 新增 `model` 参数，允许用户为 hook 评估指定自定义模型
- Plugins：新增对共享和安装 output styles 的支持

**修复**
- 修复用户设置中的 slash commands 被加载两次、可能导致渲染问题的 bug
- 修复命令描述中用户设置与项目设置标注错误的问题
- 修复插件命令 hooks 在执行期间超时导致的崩溃
- 修复：Bedrock 用户在使用 `--model haiku` 时不再在 /model 选择器中看到重复的 Opus 条目
- 修复信任对话框和引导流程中损坏的安全文档链接
- 修复按 ESC 关闭 diff 模态框时同时中断模型的问题
- 修复 ctrl-r 历史搜索落在 slash command 上时会取消搜索的问题

**改进**
- SDK：支持为 hooks 自定义超时
- 允许更多安全的 git 命令在无需批准的情况下运行
- 从 web teleport 一个会话时将自动设置上游分支

### 2.0.37

**修复**
- 修复通知的空闲判定计算方式

**改进**
- Hooks：为 Notification hook 事件新增 matcher 值
- Output Styles：在 frontmatter 中新增 `keep-coding-instructions` 选项

### 2.0.36

**修复**
- 修复：DISABLE_AUTOUPDATER 环境变量现在能正确禁用包管理器的更新通知
- 修复排队消息被错误地当作 bash 命令执行的问题
- 修复在排队消息处理期间输入丢失的问题

### 2.0.35

**改进**
- 改进搜索命令时的模糊搜索结果
- 改进 VS Code 扩展，使其在整个 UI 中遵循 `chat.fontSize` 和 `chat.fontFamily` 设置，并立即应用字体更改而无需重新加载
- 将 `ignorePatterns` 从项目配置迁移到 localSettings 中的 deny 权限

**新功能**
- 新增 `CLAUDE_CODE_EXIT_AFTER_STOP_DELAY` 环境变量，在指定空闲时长后自动退出 SDK 模式，对自动化工作流和脚本很有用

**修复**
- 修复菜单导航在含空字符串或其他 falsy 值的项目上卡住的问题（例如在 `/hooks` 菜单中）

### 2.0.34

**新功能**
- VSCode 扩展：新增设置以配置新对话的初始权限模式

**改进**
- 通过基于 Rust 的原生模糊查找器改进文件路径建议性能

**修复**
- 修复导致带 OAuth 的 MCP 服务器（例如 Slack）在连接期间挂起的无限 token 刷新循环
- 修复读取或写入大文件（尤其是 base64 编码的图像）时的内存崩溃

### 2.0.33

**改进**
- 原生二进制安装现在启动更快。

**修复**
- 修复 `claude doctor` 通过正确解析软链接来纠正错误地区分 Homebrew 与 npm-global 安装的问题
- 修复 `claude mcp serve` 暴露具有不兼容 outputSchemas 的工具的问题

### 2.0.32

**改进**
- 根据社区反馈取消弃用 output styles

**新功能**
- 新增 `companyAnnouncements` 设置，用于在启动时显示公告

**修复**
- 修复 PostToolUse hook 执行期间 hook 进度消息未正确更新的问题

### 2.0.31

**改进**
- Windows：原生安装使用 shift+tab 作为模式切换快捷键，而非 alt+m
- Vertex：在受支持的模型上新增对 Web Search 的支持
- VSCode：新增 respectGitIgnore 配置，可在文件搜索中包含 .gitignored 文件（默认为 true）

**修复**
- 修复 subagents 与 MCP 服务器相关的 "Tool names must be unique" 错误的 bug
- 修复因 `/compact` 不遵循已有的 compact 边界而导致其失败并报 `prompt_too_long` 的问题
- 修复插件卸载未移除插件的问题

### 2.0.30

**新功能**
- 当在 macOS 上 keychain 锁定导致 API key 错误时，新增运行 `security unlock-keychain` 的有用提示
- 新增 `allowUnsandboxedCommands` sandbox 设置，可在策略层面禁用 dangerouslyDisableSandbox 这一逃生通道
- 为自定义 agent 定义新增 `disallowedTools` 字段以显式屏蔽工具
- 新增基于提示词的 stop hooks
- 在原生构建上启用 SSE MCP 服务器
- VSCode：新增 respectGitIgnore 配置，可在文件搜索中包含 .gitignored 文件（默认为 true）

**改进**
- 弃用 output styles。请在 `/output-style` 中查看选项，改用 --system-prompt-file、--system-prompt、--append-system-prompt、CLAUDE.md 或 plugins
- 移除对自定义 ripgrep 配置的支持，解决了 Search 返回无结果且配置发现失败的问题

**修复**
- 修复 Explore agent 在代码库探索期间创建多余 .md 调查文件的问题
- 修复 `/context` 有时会失败并报 "max_tokens must be greater than thinking.budget_tokens" 错误消息的 bug
- 修复 `--mcp-config` 标志以正确覆盖基于文件的 MCP 配置
- 修复将会话权限保存到本地设置的 bug
- 修复 MCP 工具对 sub-agents 不可用的问题
- 修复使用 --dangerously-skip-permissions 标志时 hooks 和 plugins 未执行的问题
- 修复用方向键浏览 typeahead 建议时的延迟
- VSCode：恢复了输入页脚中显示当前文件或代码选择状态的选择指示器

### 2.0.28

**新功能**
- Plan 模式：引入新的 Plan subagent
- Subagents：claude 现在可以选择恢复 subagents
- Subagents：claude 可动态选择其 subagents 所用的模型
- SDK：新增 --max-budget-usd 标志
- 为基于 git 的 plugins 和 marketplaces 新增分支和标签支持，使用 fragment 语法（例如 `owner/repo#branch`）

**改进**
- 自定义 slash commands、subagents 和 output styles 的发现不再遵循 .gitignore
- 阻止 `/terminal-setup` 在 VS Code 中向 `Shift + Enter` 添加反斜杠

**修复**
- 修复从主目录启动时 macOS 权限提示会在初次启动时弹出的 bug
- 其他若干 bug 修复

### 2.0.27

**新功能**
- 全新的权限提示 UI
- 在会话恢复界面新增当前分支过滤和搜索，便于导航
- VSCode 扩展：新增配置项以在文件搜索中包含 .gitignored 文件

**修复**
- 修复目录 @-mention 导致 "No assistant message found" 错误的问题
- VSCode 扩展：修复无关的 'Warmup' 对话，以及配置/设置偶尔被重置为默认值的问题

### 2.0.25

**改进**
- 移除旧版 SDK 入口点。请迁移到 @anthropic-ai/claude-agent-sdk 以获得未来的 SDK 更新：https://platform.claude.com/docs/en/agent-sdk/migration-guide

### 2.0.24

**新功能**
- Claude Code Web：支持 Web -> CLI teleport
- Sandbox：在 Linux 和 Mac 上发布 BashTool 的 sandbox 模式

**修复**
- 修复指定 --setting-sources 'project' 时项目级 skills 未加载的 bug

**其他**
- Bedrock：在需要鉴权时显示 awsAuthRefresh 输出

### 2.0.22

**新功能**
- 在工具响应中支持 MCP `structuredContent` 字段
- 新增交互式问题工具
- 新增对企业托管 MCP 允许列表和拒绝列表的支持

**修复**
- 修复滚动浏览 slash commands 时的内容布局偏移
- 修复并行工具调用导致重复权限提示的 bug

**改进**
- IDE：新增启用/禁用 thinking 的开关。

### 2.0.21

**新功能**
- 在工具响应中支持 MCP `structuredContent` 字段
- 新增交互式问题工具
- claude 现在在 plan 模式中会更频繁地向你提问
- 为 Pro 用户新增 Haiku 4.5 作为模型选项

**修复**
- 修复排队命令无法访问先前消息输出的问题

### 2.0.20

**新功能**
- 新增对 Claude Skills 的支持

### 2.0.19

**改进**
- 自动将长时间运行的 bash 命令置入后台，而非将其终止。可通过 BASH_DEFAULT_TIMEOUT_MS 自定义

**修复**
- 修复在 print 模式中不必要地调用 Haiku 的 bug

### 2.0.17

**新功能**
- 在模型选择器中新增 Haiku 4.5！
- Haiku 4.5 在 plan 模式中自动使用 Sonnet，在执行时使用 Haiku（即默认 SonnetPlan）
- 引入 Explore subagent。由 Haiku 驱动，可高效搜索你的代码库以节省上下文！

**改进**
- 3P（Bedrock 和 Vertex）尚未自动升级。可通过设置 `ANTHROPIC_DEFAULT_HAIKU_MODEL` 手动升级
- OTEL：支持 HTTP_PROXY 和 HTTPS_PROXY
- `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC` 现在会禁用 release notes 的获取

### 2.0.15

**修复**
- 修复 resume 时先前创建的文件需要先重新读取才能写入的 bug
- 修复 `-p` 模式下 @-mention 的文件需要先重新读取才能写入的 bug

### 2.0.14

**修复**
- 修复 @-mention MCP 服务器以切换其开/关
- 修复 ultrathink + thinking 切换
- 减少不必要的登录

**改进**
- 改进 bash 内联环境变量的权限检查
- 补充 --system-prompt 文档
- 渲染方面的若干改进
- Plugins UI 打磨

### 2.0.13

**修复**
- 修复 `/plugin` 在原生构建上不工作的问题

### 2.0.12

**新功能**
- **插件系统发布**：通过来自 marketplaces 的自定义命令、agents、hooks 和 MCP 服务器扩展 Claude Code
- `/plugin install`、`/plugin enable/disable`、`/plugin marketplace` 命令用于插件管理
- 通过 `extraKnownMarketplaces` 进行仓库级插件配置，便于团队协作
- `/plugin validate` 命令用于校验插件结构和配置
- 插件公告博客文章见 https://www.anthropic.com/news/claude-code-plugins
- 插件文档见 https://code.claude.com/docs/en/plugins
- 通过 `/doctor` 命令提供全面的错误消息和诊断

**改进**
- 避免 `/model` 选择器中的闪烁
- 改进 `/help`
- 避免在 `/resume` 摘要中提及 hooks
- `/config` 中 "verbose" 设置的更改现在会跨会话持久化

### 2.0.11

**改进**
- 将系统提示词大小减少 1.4k tokens

**修复**
- IDE：修复键盘快捷键和焦点问题，使交互更流畅
- 修复 Opus 回退限速错误错误地出现的问题
- 修复 /add-dir 命令选择错误默认标签页的问题

### 2.0.10

**新功能**
- 通过 @mention 或在 /mcp 中启用/禁用 MCP 服务器
- 在 bash 模式中新增 shell 命令的 tab 补全
- 按 Ctrl-G 在系统配置的文本编辑器中编辑你的提示词

**改进**
- 重写终端渲染器，带来丝般顺滑的 UI
- PreToolUse hooks 现在可以修改工具输入

**修复**
- 修复命令中含环境变量时 bash 权限检查的问题

### 2.0.9

**修复**
- 修复 bash 后台运行停止工作的回退问题

### 2.0.8

**改进**
- 将 Bedrock 默认 Sonnet 模型更新为 `global.anthropic.claude-sonnet-4-5-20250929-v1:0`
- 改进对在深色终端上使用浅色主题用户的消息渲染
- 移除已弃用的 .claude.json allowedTools、ignorePatterns、env 和 todoFeatureEnabled 配置选项（改为在你的 settings.json 中配置这些）

**新功能**
- IDE：新增对话中文件和文件夹的拖放支持

**修复**
- /context：修复 thinking 块的计数

### 2.0.5

**修复**
- IDE：修复 IME 用 Enter 和 Tab 意外提交消息的问题
- 修复未处理的 OAuth 过期 401 API 错误

**新功能**
- IDE：在登录界面新增 "Open in Terminal" 链接
- SDK：新增 SDKUserMessageReplay.isReplay 以防止重复消息

### 2.0.1

**修复**
- 跳过 Bedrock 和 Vertex 的 Sonnet 4.5 默认模型设置更改
- 若干 bug 修复和呈现改进

### 2.0.0

**新功能**
- 全新原生 VS Code 扩展
- /rewind 一段对话以撤销代码更改
- /usage 命令以查看 plan 限制
- Tab 切换 thinking（跨会话保持）
- Ctrl-R 搜索历史
- 通过 `--agents` 标志动态添加 subagents

**改进**
- 全 App 焕然一新的视觉外观
- Hooks：减少 PostToolUse 出现 'tool_use' ids were found without 'tool_result' blocks 错误
- SDK：Claude Code SDK 现已成为 Claude Agent SDK

**其他**
- 卸下了 claude config 命令
