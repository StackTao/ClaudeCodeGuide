## v2.1.x 系列（2.1.25 – 2.1.0）

### 2.1.25

**修复**
- 修复 Bedrock 和 Vertex 上网关用户的 beta header 校验错误，确保 `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1` 避免该错误。

### 2.1.23

**新功能**
- 新增可自定义 spinner 动词设置（`spinnerVerbs`）。

**改进**
- 改进终端渲染性能，优化屏幕数据布局。
- 把 Bash 命令改为在已用时间旁显示超时时长。
- 把已合并的 pull request 改为在提示页脚显示紫色状态指示。

**修复**
- 修复企业代理后或使用客户端证书用户的 mTLS 和代理连接。
- 修复逐用户临时目录隔离以防止共享系统上的权限冲突。
- 修复启用 prompt 缓存范围时可能导致 400 错误的竞态条件。
- 修复 headless 流式会话结束时待处理异步 hook 未取消的问题。
- 修复接受建议时 tab 补全不更新输入字段的问题。
- 修复 ripgrep 搜索超时静默返回空结果而非报错的问题。
- [IDE] 修复 headless 模式下 Bedrock 用户的模型选项显示错误区域字符串的问题。

### 2.1.22

**修复**
- 修复非交互（-p）模式的 structured outputs。

### 2.1.21

**新功能**
- 新增选项选择提示中日语 IME 全角（zenkaku）数字输入支持。

**改进**
- 改进读/搜进度指示，进行中显示「Reading…」、完成显示「Read」。
- 改进 Claude 偏好文件操作工具（Read、Edit、Write）而非 bash 等价物（cat、sed、awk）。
- [VSCode] 新增自动 Python 虚拟环境激活，确保 `python` 和 `pip` 命令使用正确解释器（可通过 `claudeCode.usePythonEnvironment` 设置配置）。

**修复**
- 修复退出时 shell 补全缓存文件被截断的问题。
- 修复恢复工具执行期间被打断会话时的 API 错误。
- 修复大输出 token 上限模型上 auto-compact 触发过早的问题。
- 修复任务 ID 删除后可能被重用的问题。
- 修复 Windows 上 VS Code 扩展中文件搜索不工作的问题。
- [VSCode] 修复消息操作按钮背景色不正确的问题。

### 2.1.20

**新功能**
- 新增 vim normal 模式下光标无法继续移动时的方向键历史导航。
- 为帮助菜单新增外部编辑器快捷键（Ctrl+G）以提高可发现性。
- 为提示页脚新增 PR review 状态指示，把当前分支 PR 状态（approved、changes requested、pending 或 draft）显示为带可点击链接的彩色点。
- 新增从 `--add-dir` 标志指定的附加目录加载 `CLAUDE.md` 文件的支持（需设 `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1`）。
- 新增通过 `TaskUpdate` 工具删除任务的能力。

**改进**
- 改进 `/sandbox` 命令 UI，依赖缺失时显示依赖状态和安装说明。
- 改进 thinking 状态文本，带微妙微光动画。
- 改进任务列表，根据终端高度动态调整可见项。
- 改进 fork 对话提示，显示如何恢复原会话。
- 把折叠读/搜分组改为进行中显示现在时（「Reading」、「Searching for」）、完成显示过去时（「Read」、「Searched for」）。
- 把 `ToolSearch` 结果改为显示为简短通知而非对话内联。
- 把 `/commit-push-pr` skill 改为在通过 MCP 工具配置时自动把 PR URL 发到 Slack 频道。
- 把 `/copy` 命令改为对所有用户可用。
- 把后台 agent 改为启动前提示工具权限。
- 把 `Bash(*)` 等权限规则改为接受并视为等价于 `Bash`。
- 把配置备份改为带时间戳并轮换（保留最近 5 个）以防数据丢失。

**修复**
- 修复可能导致 resume 加载完整历史而非压缩摘要的会话压缩问题。
- 修复 agent 有时忽略活动工作时发送的用户消息的问题。
- 修复宽字符（emoji、CJK）被更窄字符替换时尾列未清除的渲染瑕疵。
- 修复 MCP 工具响应含特殊 Unicode 字符时的 JSON 解析错误。
- 修复多行和换行文本输入中上/下方向键优先光标移动而非历史导航的问题。
- 修复按上方向键导航命令历史时草稿提示丢失的问题。
- 修复输入中途键入 slash 命令时 ghost text 闪烁的问题。
- 修复 marketplace 源移除未正确删除设置的问题。
- 修复 `/context` 等某些命令重复输出的问题。
- 修复任务列表有时显示在主对话视图外的问题。
- 修复 Python docstring 等多行结构内 diff 语法高亮的问题。
- 修复取消工具使用时的崩溃。

### 2.1.19

**新功能**
- 新增环境变量 `CLAUDE_CODE_ENABLE_TASKS`，设为 `false` 以临时保留旧系统。
- 为自定义命令新增简写 `$0`、`$1` 等以访问单个参数。
- [SDK] 启用 `replayUserMessages` 时新增把 `queued_command` 附件消息重放为 `SDKUserMessageReplay` 事件。
- [VSCode] 为所有用户启用会话 fork 和 rewind 功能。

**改进**
- 把无额外权限或 hook 的 skill 改为无需批准即允许。
- 把索引参数语法从 `$ARGUMENTS.0` 改为 `$ARGUMENTS[0]`（括号语法）。

**修复**
- 修复无 AVX 指令支持的处理器上的崩溃。
- 修复终端关闭时悬空 Claude Code 进程的问题，方法是捕获 `process.exit()` 的 EIO 错误并用 SIGKILL 回退。
- 修复从不同目录（如 git worktree）恢复时 `/rename` 和 `/tag` 未更新正确会话的问题。
- 修复从不同目录运行时按自定义标题恢复会话不工作的问题。
- 修复使用提示 stash（Ctrl+S）和恢复时粘贴文本内容丢失的问题。
- 修复 agent 列表对无显式模型设置的 agent 显示「Sonnet (default)」而非「Inherit (default)」的问题。
- 修复后台化 hook 命令不提前返回、可能导致会话等待有意后台化进程的问题。
- 修复文件写预览省略空行的问题。

### 2.1.18

**新功能**
- 新增可自定义键盘快捷键。按上下文配置键位、创建 chord 序列并个性化你的工作流。运行 `/keybindings` 开始。详见 https://code.claude.com/docs/en/keybindings

### 2.1.17

**修复**
- 修复无 AVX 指令支持的处理器上的崩溃。

### 2.1.16

**新功能**
- 新增任务管理系统，包括依赖跟踪等新能力。
- [VSCode] 新增原生插件管理支持。
- [VSCode] 新增 OAuth 用户从 Sessions 对话框浏览和恢复远程 Claude 会话的能力。

**修复**
- 修复恢复重度使用 subagent 会话时的内存溢出崩溃。
- 修复运行 `/compact` 后「context remaining」警告未隐藏的问题。
- 修复 resume 屏幕上会话标题不遵循用户语言设置的问题。
- [IDE] 修复 Windows 上启动时 Claude Code 侧栏视图容器不出现的竞态条件。

### 2.1.15

**新功能**
- 为 npm 安装新增弃用通知——运行 `claude install` 或见 https://docs.anthropic.com/en/docs/claude-code/getting-started 了解更多选项。

**改进**
- 用 React Compiler 改进 UI 渲染性能。

**修复**
- 修复运行 `/compact` 后「Context left until auto-compact」警告不消失的问题。
- 修复 MCP stdio server 超时不杀子进程、可能导致 UI 冻结的问题。

### 2.1.14

**新功能**
- 在 bash 模式（`!`）新增基于历史的自动补全——键入部分命令并按 Tab 从你的 bash 命令历史补全。
- 为已安装插件列表新增搜索——键入以按名称或描述筛选。
- 新增把插件固定到特定 git commit SHA 的支持，允许 marketplace 条目安装精确版本。
- [VSCode] 新增 `/usage` 命令以显示当前 plan 用量。

**改进**
- 改进 backspace 以把粘贴文本作为单个 token 删除而非一次一字符。

**修复**
- 修复上下文窗口阻止限制计算过于激进、在约 65% 上下文使用时阻止用户而非预期约 98% 的回归。
- 修复运行并行 subagent 时可能导致崩溃的内存问题。
- 修复 shell 命令完成后流资源未清理导致长会话内存泄漏的问题。
- 修复 bash 模式下 `@` 符号错误触发文件自动补全建议的问题。
- 修复 `@`-mention 菜单文件夹点击行为导航进目录而非选择它们的问题。
- 修复描述很长时 `/feedback` 命令生成无效 GitHub issue URL 的问题。
- 修复 verbose 模式下 `/context` 命令显示与状态行相同 token 计数和百分比的问题。
- 修复 `/config`、`/context`、`/model` 和 `/todos` 命令覆盖层可能意外关闭的问题。
- 修复键入相似命令（如 `/context` vs `/compact`）时 slash 命令自动补全选错命令的问题。
- 修复仅配置一个 marketplace 时插件 marketplace 中返回导航不一致的问题。
- 修复 iTerm2 进度条退出时未正确清除、导致残留指示器和铃声的问题。

### 2.1.12

**修复**
- 修复消息渲染 bug。

### 2.1.11

**修复**
- 修复 HTTP/SSE 传输的过多 MCP 连接请求。

### 2.1.10

**新功能**
- 新增 `Setup` hook 事件，可通过 `--init`、`--init-only` 或 `--maintenance` CLI 标志触发，用于仓库设置和维护操作。
- 新增键盘快捷键「c」，在登录期间浏览器未自动打开时复制 OAuth URL。
- [VSCode] 为插件列表新增安装数显示。
- [VSCode] 安装插件时新增信任警告。

**改进**
- 改进启动以捕获 REPL 完全就绪前键入的按键。
- 改进文件建议，接受时显示为可移除附件而非插入文本。

**修复**
- 修复运行含 `${index + 1}` 等 JavaScript 模板字面量 heredoc 的 bash 命令时的崩溃。

### 2.1.9

**新功能**
- 新增 `auto:N` 语法以配置 MCP 工具搜索自动启用阈值，N 为上下文窗口百分比（0-100）。
- 新增 `plansDirectory` 设置以自定义 plan 文件存储位置。
- 在 AskUserQuestion「Other」输入字段新增外部编辑器支持（Ctrl+G）。
- 为从 web 会话创建的 commit 和 PR 新增会话 URL 归因。
- 新增 `PreToolUse` hook 向模型返回 `additionalContext` 的支持。
- 为 skill 新增 `${CLAUDE_SESSION_ID}` 字符串替换以访问当前会话 ID。

**修复**
- 修复带并行工具调用的长会话因孤立 tool_result 块以 API 错误失败的问题。
- 修复缓存连接 promise 从不 resolve 时 MCP server 重连挂起的问题。
- 修复使用 Kitty 键盘协议的终端（Ghostty、iTerm2、kitty、WezTerm）中 Ctrl+Z 挂起不工作的问题。

### 2.1.7

**新功能**
- 新增 `showTurnDuration` 设置以隐藏回合时长消息（如「Cooked for 1m 6s」）。
- 新增接受权限提示时提供反馈的能力。
- 在任务通知中新增 agent 最终响应的内联显示，使无需读完整 transcript 文件即可看到结果。

**改进**
- 改进键入响应性，减少终端渲染中的内存分配开销。
- 为所有用户默认启用 MCP 工具搜索 auto 模式。当 MCP 工具描述超过上下文窗口 10% 时，自动延迟并通过 MCPSearch 工具发现而非预先加载。这减少配置了许多 MCP 工具用户的上下文使用。用户可通过在设置中向 `disallowedTools` 添加 `MCPSearch` 禁用。
- 把 OAuth 和 API Console URL 从 console.anthropic.com 改为 platform.claude.com。

**安全**
- 修复通配权限规则可匹配含 shell 操作符复合命令的安全漏洞。

**修复**
- 修复云同步工具、杀毒扫描器或 Git 在不改内容下触碰文件时间戳时 Windows 上虚假「file modified」错误。
- 修复流式执行期间兄弟工具失败时孤立 tool_result 错误。
- 修复上下文窗口阻止限制用完整上下文窗口而非有效上下文窗口（为最大输出 token 预留空间）计算的问题。
- 修复运行 `/model` 或 `/theme` 等本地 slash 命令时 spinner 短暂闪烁的问题。
- 修复终端标题动画抖动，方法是用固定宽度 braille 字符。
- 修复带 git submodule 插件安装时未完全初始化的问题。
- 修复临时目录路径含 `t` 或 `n` 等被误解为转义序列字符时 Windows 上 bash 命令失败的问题。
- [VSCode] 修复 `claudeProcessWrapper` 设置传递 wrapper 路径而非 Claude 二进制路径的问题。

### 2.1.6

**新功能**
- 为 `/config` 命令新增搜索功能以快速筛选设置。
- 为 `/doctor` 新增 Updates 部分，显示自动更新频道和可用 npm 版本（stable/latest）。
- 为 `/stats` 命令新增日期范围筛选——按 `r` 在最近 7 天、最近 30 天和全部时间间循环。
- 新增在子目录中处理文件时从嵌套 `.claude/skills` 目录自动发现 skill。
- 为状态行输入新增 `context_window.used_percentage` 和 `context_window.remaining_percentage` 字段以更易显示上下文窗口。
- 新增 Ctrl+G 期间编辑器失败时的错误显示。

**改进**
- 改进外部 CLAUDE.md 导入批准对话框以显示正在导入哪些文件及来源。
- 改进 `/tasks` 对话框，仅有一个后台任务运行时直接进入任务详情。
- 改进 @ 自动补全，为不同建议类型加图标和单行格式。
- 更新「Help improve Claude」设置获取，失败时刷新 OAuth 并重试。
- 把任务通知显示改为多个后台任务同时完成时上限 3 行带溢出摘要。
- 把终端标题改为启动时设为「Claude Code」以更好识别窗口。
- 移除 @-mention MCP server 以启用/禁用的能力——改用 `/mcp enable <name>`。
- [VSCode] 修复手动压缩后用量指示器不更新的问题。

**安全**
- 修复通过 shell 行续行的权限绕过，它可能允许被阻止命令执行。

**修复**
- 修复文件监视器在不改内容下触碰文件时虚假「File has been unexpectedly modified」错误。
- 修复多行响应中文本样式（粗体、颜色）逐渐错位的问题。
- 修复在描述字段键入「n」时反馈面板意外关闭的问题。
- 修复每周重置后低用量时出现速率限制警告的问题（现在需 70% 用量）。
- 修复恢复先前会话时速率限制选项菜单错误自动打开的问题。
- 修复 Kitty 键盘协议终端中数字键盘键输出转义序列而非字符的问题。
- 修复 Kitty 键盘协议终端中 Option+Return 不插入换行的问题。
- 修复主目录中损坏配置备份文件累积的问题（现在每个配置文件仅创建一个备份）。
- 修复 `mcp list` 和 `mcp get` 命令留下孤立 MCP server 进程的问题。
- 修复 ink2 模式下节点通过 `display:none` 隐藏时的视觉瑕疵。

### 2.1.5

**新功能**
- 新增 `CLAUDE_CODE_TMPDIR` 环境变量以覆盖用于内部临时文件的临时目录，适用于有自定义临时目录要求的环境。

### 2.1.4

**新功能**
- 新增 `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS` 环境变量以禁用所有后台任务功能，包括自动后台化和 Ctrl+B 快捷键。

**修复**
- 修复「Help improve Claude」设置获取因过期 OAuth token 失败时刷新 OAuth 并重试。

### 2.1.3

**新功能**
- 合并 slash 命令和 skill，简化心智模型且行为不变。
- 为 `/config` 新增发布频道（`stable` 或 `latest`）切换。
- 新增不可达权限规则的检测和警告，在 `/doctor` 中和保存规则后给出警告，包含每条规则来源和可操作修复指引。

**改进**
- 改进终端渲染稳定性，防止失控写入损坏光标状态。
- 改进 slash 命令建议可读性，把长描述截断到 2 行。
- 把工具 hook 执行超时从 60 秒改为 10 分钟。
- [VSCode] 为权限请求新增可点击目标选择器，允许你选择设置保存位置（本项目、所有项目、与团队共享或仅会话）。

**修复**
- 修复 plan 文件跨 `/clear` 命令保留的问题，现在确保清除对话后使用全新 plan 文件。
- 修复大 inode 文件系统（如 ExFAT）上虚假 skill 重复检测，方法是对 inode 值用 64 位精度。
- 修复状态栏后台任务计数与任务对话框显示项不匹配的问题。
- 修复对话压缩期间 sub-agent 使用错误模型的问题。
- 修复 sub-agent 中网页搜索使用错误模型的问题。
- 修复从主目录运行时信任对话框接受未启用 hook 等需信任功能的问题。

### 2.1.2

**新功能**
- 为拖到终端的图像新增源路径元数据，帮助 Claude 理解图像来源。
- 在支持 OSC 8 的终端（如 iTerm）中为工具输出文件路径新增可点击超链接。
- 新增 Windows Package Manager（winget）安装支持，带自动检测和更新说明。
- 在 plan 模式新增 Shift+Tab 快捷键以快速选择「auto-accept edits」选项。
- 新增 `FORCE_AUTOUPDATE_PLUGINS` 环境变量以允许主自动更新器禁用时仍插件自动更新。
- 为 SessionStart hook 输入新增 `agent_type`，指定 `--agent` 时填充。

**改进**
- 改进 macOS 上 Option-as-Meta 提示，为 iTerm2、Kitty、WezTerm 等原生 CSIu 终端显示终端专属说明。
- 改进 SSH 上粘贴图像时的错误消息，建议用 `scp` 而非无用的剪贴板快捷键提示。
- 改进权限解释器，不把日常开发工作流（git fetch/rebase、npm install、测试、PR）标记为中风险。
- 把大 bash 命令输出改为保存到磁盘而非截断，允许 Claude 读取完整内容。
- 把大工具输出改为持久化到磁盘而非截断，通过文件引用提供完整输出访问。
- 把 `/plugins` installed 标签改为统一插件和 MCP 并按范围分组。
- 弃用 Windows managed 设置路径 `C:\ProgramData\ClaudeCode\managed-settings.json`——管理员应迁移到 `C:\Program Files\ClaudeCode\managed-settings.json`。
- [SDK] 把最低 zod peer 依赖改为 ^4.0.0。

**安全**
- 修复 bash 命令处理中畸形输入可执行任意命令的命令注入漏洞。
- 修复 MCP 工具名通过净化用户专属 server 配置在分析事件中暴露的问题。

**修复**
- 修复 tree-sitter 解析树未释放、导致长会话 WASM 内存无界增长的内存泄漏。
- 修复在 CLAUDE.md 文件中用 `@include` 指令时二进制文件（图像、PDF 等）意外被纳入内存的问题。
- 修复更新错误声称另一安装正在进行的问题。
- 修复监视目录中存在 socket 文件时的崩溃（对 EOPNOTSUPP 错误的纵深防御）。
- 修复使用 `/tasks` 命令时远程会话 URL 和 teleport 损坏的问题。
- [VSCode] 修复手动压缩后用量显示不更新的问题。

### 2.1.0

**新功能**
- 新增自动 skill 热重载——在 `~/.claude/skills` 或 `.claude/skills` 创建或修改的 skill 现在立即可用，无需重启会话。
- 新增用 skill frontmatter 中 `context: fork` 在 fork 的 sub-agent 上下文中运行 skill 和 slash 命令的支持。
- 新增 skill 中 `agent` 字段以指定执行的 agent 类型。
- 新增 `language` 设置以配置 Claude 响应语言（如 language: "japanese"）。
- 新增 `settings.json` 中 `respectGitignore` 支持以按项目控制 @-mention 文件选择器行为。
- 新增 `IS_DEMO` 环境变量以从 UI 隐藏邮箱和组织，适用于直播或录制会话。
- 新增 Bash 工具权限的通配模式匹配，在规则任意位置用 `*`（如 `Bash(npm *)`、`Bash(* install)`、`Bash(git * main)`）。
- 新增统一的 bash 命令和 agent 的 Ctrl+B 后台化——按 Ctrl+B 现在同时后台化所有运行的前台任务。
- 新增 MCP `list_changed` 通知支持，允许 MCP server 动态更新其可用工具、提示和资源而无需重连。
- 为 claude.ai 订阅者新增 `/teleport` 和 `/remote-env` slash 命令，允许恢复和配置远程会话。
- 新增在 settings.json 权限或 `--disallowedTools` CLI 标志中用 `Task(AgentName)` 语法禁用特定 agent 的支持。
- 为 agent frontmatter 新增 hook 支持，允许 agent 定义作用于 agent 生命周期的 PreToolUse、PostToolUse 和 Stop hook。
- 为 skill 和 slash 命令 frontmatter 新增 hook 支持。
- 新增 Vim 动作：`;` 和 `,` 重复 f/F/t/T 动作、`y` 操作符配合 `yy`/`Y` 进行 yank、`p`/`P` 粘贴、文本对象（`iw`、`aw`、`iW`、`aW`、`i"`、`a"`、`i'`、`a'`、`i(`、`a(`、`i[`、`a[`、`i{`、`a{`）、`>>` 和 `<<` 缩进/取消缩进、以及 `J` 连接行。
- 新增 `/plan` 命令快捷方式以直接从提示启用 plan 模式。
- 新增 `/` 出现在输入任意位置（而非仅开头）时的 slash 命令自动补全支持。
- 新增交互模式下 `--tools` 标志支持以限制交互会话期间 Claude 可用哪些内置工具。
- 新增 `CLAUDE_CODE_FILE_READ_MAX_OUTPUT_TOKENS` 环境变量以覆盖默认文件读取 token 限制。
- 新增 hook 的 `once: true` 配置支持。
- 新增 frontmatter `allowed-tools` 字段中 YAML 风格列表支持以更清晰声明 skill。
- 新增来自插件的 prompt 和 agent hook 类型支持（此前仅支持 command hook）。
- 新增 iTerm2 中图像粘贴的 Cmd+V 支持（映射到 Ctrl+V）。
- 新增对话框中左/右方向键导航以循环标签。
- 新增 Ctrl+O transcript 模式中的实时 thinking 块显示。
- 在后台 bash 任务详情对话框中新增完整输出的文件路径。
- 在上下文可视化中新增 Skills 作为单独类别。

**安全**
- 修复敏感数据（OAuth token、API key、密码）可能在调试日志中暴露的安全问题。

**改进**
- 改进 CLI 帮助输出，按字母顺序显示选项和子命令以便导航。
- 改进终端渲染性能（使用原生安装器或 Bun 时），尤其对含 emoji、ANSI 码和 Unicode 字符的文本。
- 改进读取多 cell Jupyter notebook 的性能。
- 改进 `cat refactor.md | claude` 等管道输入的可靠性。
- 改进 AskQuestion 工具的可靠性。
- 改进 sed 原地编辑命令以渲染为带 diff 预览的文件编辑。
- 改进 Claude 在响应因输出 token 限制被截断时自动继续，而非显示错误消息。
- 改进压缩可靠性。
- 改进 subagent（Task 工具）在权限拒绝后继续工作，允许其尝试替代方法。
- 改进 skill 在执行时显示进度，随发生显示工具使用。
- 改进 `/skills/` 目录的 skill 默认在 slash 命令菜单可见（用 frontmatter 中 `user-invocable: false` 退出）。
- 改进 skill 建议以优先最近和频繁使用的 skill。
- 改进等待首个响应 token 时的 spinner 反馈。
- 改进 spinner 中 token 计数显示以包含后台 agent 的 token。
- 改进异步 agent 的增量输出，给主线程更多控制和可见性。
- 改进权限提示 UX，Tab 提示移到页脚、更干净的 Yes/No 输入标签带上下文占位符。
- 改进 Claude in Chrome 通知，缩短帮助文本并持久显示至关闭。
- 改进 macOS 截图粘贴可靠性，带 TIFF 格式支持。
- 改进 `/stats` 输出。
- 更新 Atlassian MCP 集成以使用更可靠的默认配置（streamable HTTP）。
- 把「Interrupted」消息颜色从红色改为灰色以减少警示感。
- 移除进入 plan 模式时的权限提示——用户现在无需批准即可进入 plan 模式。
- 移除图像引用链接的下划线样式。
- [SDK] 把最低 zod peer 依赖改为 ^4.0.0。
- [VSCode] 为上下文菜单新增当前选中模型名。
- [VSCode] 为 auto-accept 权限按钮新增描述性标签（如「Yes, allow npm for this project」而非「Yes, and don't ask again」）。

**修复**
- 修复服务器报告 token 过期但本地过期检查不一致时 OAuth token 刷新不触发的问题。
- 修复瞬时服务器错误后会话持久化卡住的问题，方法是在条目实际已存储时从 409 冲突恢复。
- 修复并发工具执行期间孤立 tool result 导致会话恢复失败的问题。
- 修复并发 token 刷新尝试期间可能从 keychain 缓存读取过期 OAuth token 的竞态条件。
- 修复 AWS Bedrock subagent 不继承 EU/APAC 跨区域推理模型配置、导致 IAM 权限限定特定区域时 403 错误的问题。
- 修复后台任务产生大输出时 API 上下文溢出，方法是截断到 30K 字符带文件路径引用。
- 修复读取 FIFO 文件时的挂起，方法是对特殊文件类型跳过符号链接解析。
- 修复 Ghostty、iTerm2、Kitty 和 WezTerm 中退出时终端键盘模式未重置的问题。
- 修复 iTerm2、Ghostty、Kitty 和 WezTerm 中 Alt+B 和 Alt+F（单词导航）不工作的问题。
- 修复插件 `allowed-tools` frontmatter 中 `${CLAUDE_PLUGIN_ROOT}` 未替换、导致工具错误要求批准的问题。
- 修复 Write 工具创建的文件使用硬编码 0o600 权限而非遵循系统 umask 的问题。
- 修复带 `$()` 命令替换的命令以解析错误失败的问题。
- 修复带反斜杠续行的多行 bash 命令被错误拆分并标记权限的问题。
- 修复 bash 命令前缀提取以正确识别全局选项后的子命令（如 `git -C /path log` 现在正确匹配 `Bash(git log:*)` 规则）。
- 修复作为 CLI 参数传入的 slash 命令（如 `claude /context`）未正确执行的问题。
- 修复 Tab 补全 slash 命令后按 Enter 选择不同命令而非提交补全命令的问题。
- 修复键入带参数命令时 slash 命令参数提示闪烁和显示不一致的问题。
- 修复直接运行 slash 命令时 Claude 有时冗余调用 Skill 工具的问题。
- 修复 `/context` 中 skill token 估算以准确反映仅 frontmatter 加载的问题。
- 修复 subagent 有时默认不继承父模型的问题。
- 修复 Bedrock/Vertex 用户使用 `--model haiku` 时模型选择器显示错误选择的问题。
- 修复权限请求选项标签中出现重复 Bash 命令的问题。
- 修复后台任务完成时输出嘈杂的问题——现在显示干净完成消息而非原始输出。
- 修复后台任务完成通知以带项目符号主动出现。
- 修复 fork 的 slash 命令取消时显示「AbortError」而非「Interrupted」消息的问题。
- 修复关闭权限对话框后光标消失的问题。
- 修复 `/hooks` 菜单滚动到不同选项时选错 hook 类型的问题。
- 修复按 Esc 取消时排队提示中图像显示为「[object Object]」的问题。
- 修复后台化任务时排队消息中图像被静默丢弃的问题。
- 修复大粘贴图像以「Image was too large」错误失败的问题。
- 修复含 CJK 字符（日语、中文、韩语）多行提示中额外空行的问题。
- 修复用户提示文本换行到多行时 ultrathink 关键词高亮应用到错误字符的问题。
- 修复 thinking 块流中途出现时折叠「Reading X files…」指示器错误切换到过去时的问题。
- 修复 bash 读命令（如 `ls` 和 `cat`）未计入折叠读/搜分组、导致分组错误显示「Read 0 files」的问题。
- 修复 spinner token 计数器以正确累积执行期间 subagent 的 token。
- 修复 git diff 解析中切片字符串保留大父字符串的内存泄漏。
- 修复 LSP 工具启动期间可能返回「no server available」的竞态条件。
- 修复网络请求超时时反馈提交无限挂起的问题。
- 修复插件发现和日志选择器视图中搜索模式按上方向键时退出的问题。
- 修复 hook 无输出时 hook 成功消息显示尾随冒号的问题。
- 修复 `-c` 或 `--resume` 恢复会话时文件和 skill 未正确发现的问题。
- 修复用上方向键或 Ctrl+R 搜索从历史重放提示时粘贴内容丢失的问题。
- 修复带排队提示的 Esc 键仅移到输入而不取消运行任务的问题。
- 减少复杂 bash 命令的权限提示。
- 修复命令搜索以优先命令名上的精确和前缀匹配而非描述中的模糊匹配。
- 修复 PreToolUse hook 在返回 `ask` 权限决定时允许 `updatedInput`，使 hook 可作为中间件同时仍请求用户同意。
- 修复基于文件的 marketplace 源的插件路径解析。
- 修复无 LSP server 配置时 LSP 工具被错误启用的问题。
- 修复名字含点的仓库后台任务以「git repository not found」错误失败的问题。
- 修复 WSL 环境的 Claude in Chrome 支持。
- 修复 Windows 原生安装器在可执行文件创建失败时静默失败的问题。
- [VSCode] 修复 markdown 内容中段落分隔不渲染的问题。
- [VSCode] 修复扩展中滚动无意间滚动父 iframe 的问题。
- [Windows] 修复渲染不正确的问题。
